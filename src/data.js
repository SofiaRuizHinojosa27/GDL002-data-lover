// esta es una función de ejemplo
// puedes ver como agregamos la función a nuestro objeto global window

const example = () => {
  return 'example';
};

window.example = example;


const motoAccident = INJURIES.filter(moto => (moto.Total_Injured_Persons_Motorcyclists));
console.log(motoAccident); //esto nos mostro todos los arreglos de la base de datos pero no especificamente los accidentes de moto 


